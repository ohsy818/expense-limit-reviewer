---
name: expense-limit-reviewer
description: 경비 청구 건의 규정·한도 검토와, 최신 입력 재평가 기반의 조건부 DB 등록까지 아우르는 표준 절차. 신청 정보 최신화 → 규칙엔진으로 승인구분 재평가 → 승인구분이 ‘한도내 승인’일 때만 안전한 DB 등록(컨텍스트/멱등성/트랜잭션/바인딩) → 단건 요약 표 산출 및 통지까지 일련의 베스트프랙티스를 정의한다. 한도 계산 목적의 DB 직접 조회는 금지하되, 등록 멱등성 확인을 위한 기등록 조회는 허용한다.
---

# expense-limit-reviewer

본 스킬은 경비 청구 건에 대해 규정·한도 검토를 수행하고, 최신 입력값으로 승인구분을 재평가한 뒤 ‘한도내 승인’인 경우에 한해 안전하게 경비 대장(expense_ledger)에 단건 등록하는 표준 절차를 제공한다. 결과는 항상 건별 1행 요약 표로 반환한다.

## 언제 쓰나
- 경비 지출결의 프로세스에서 ‘규정·한도 검토’와 그 결과에 따른 ‘경비 대장 단건 등록’을 연계 수행할 때.
- 단일 청구 건을 4시간 이내 DB 등록하고, 등록/보류 결과를 표준 포맷으로 통지해야 할 때.

## 입력(표준)
- 필수: tenant_id, account_code(계정과목 코드), applicant(신청자 식별자), used_at(사용일, ISO-8601), amount(수치), currency(KRW 등)
- 권장: purpose(사용 목적/메모), team/department, has_receipt, evidences(첨부 목록)
- 워크아이템 컨텍스트: proc_inst_id, todo_id (DB 등록 시 필수)
- 매핑 규칙: 자유서술형 경비유형은 내부 account_code로 정규화. 미확정 시 ‘유형 확인 필요’로 보류.

## 핵심 원칙
- 한도 판단은 반드시 비즈니스 규칙엔진 evaluate(rule='경비 한도 승인 판단')로 수행한다.
  - 한도 계산 목적의 DB 직접 조회(expense_limit/expense_ledger 합산 등)는 금지.
- 등록 운영을 위한 DB 접근(멱등성 확인용 기등록 조회, INSERT)은 허용하되, 준비문/파라미터 바인딩과 트랜잭션을 반드시 사용한다.
- 결과 출력물에는 내부 스키마/테이블명/SQL/보안정책(RLS)·도구명 등을 노출하지 않는다(내부 로그만 허용).
- 모든 결과는 건별 1행 표로 반환하며, 단문 알림만 보내지 않는다.

## 절차 A. 작업 절차 표준화(신규/개선)
1) 입력 최신화(개선)
   - 현재 워크아이템 컨텍스트와 신청서에서 다음 입력을 수집·정규화: account_code, amount, used_at, purpose, applicant, team/department, tenant_id 등.
   - 필수값 누락 시 즉시 보류로 분기하고 사유를 기록: 예) ‘필수 입력 누락: account_code, used_at’. 
   - 문자열 길이·형식 사전 검증(예: 금액 숫자형, 날짜 ISO-8601, purpose 길이 등).

2) 승인 룰 재평가(신규)
   - DB 등록 직전, 최신 입력으로 규칙엔진 evaluate(rule='경비 한도 승인 판단')를 호출해 승인구분을 확정.
   - 응답 필드 예: approval(자동승인/팀장승인), is_over_limit, limit_amount, period_type/period_range, used_amount_to_date, projected_total, over_amount, rule_name, rule_version, evaluated_at.
   - 승인구분(룰 산출) 매핑: 
     - is_over_limit=true → ‘한도 초과’
     - 규칙 호출 오류/타임아웃/미정의 → ‘한도 미정의/판단불가’
     - 그 외 → ‘한도내 승인’

3) 조건부 등록(개선)
   - 승인구분이 ‘한도내 승인’일 때만 DB 등록 진행.
   - ‘한도 초과’ 또는 ‘한도 미정의/판단불가’는 등록을 보류하고 사유를 표에 기록(초과 근거, 미정의 항목, 입력 누락 등).

4) 결과 산출물(신규)
   - 처리 결과는 건별 1행 요약 표로 반환한다(단일 건도 동일 포맷). 
   - 표 열: 계정과목 | 금액 | 사용일 | 승인구분(룰 산출) | 등록상태(성공/보류) | 사유/메모(선택)

## 절차 B. 안전한 등록 실행(개선)
5) 컨텍스트 바인딩(신규)
   - 현재 워크아이템에서 proc_inst_id, todo_id를 해소한다.
   - 둘 중 하나라도 없거나 템플릿 플레이스홀더('{{...}}') 형태면 DB 쓰기를 시도하지 말고 보류 처리(사유: 워크아이템 식별자 없음/플레이스홀더 감지).

6) 멱등성 체크(개선)
   - 동일 키(tenant_id, applicant, account_code, amount, used_at, purpose)로 기등록 여부를 사전 조회한다.
   - 기등록이면 재삽입 금지. 정책에 따라 결과 표의 등록상태를 ‘성공(기등록)’ 또는 ‘보류(중복)’로 표기.

7) 파라미터 바인딩(개선)
   - SQL은 반드시 준비문/파라미터 바인딩 방식으로 실행한다.
   - 문자열 길이 제한(예: purpose 등)을 사전 확인해 초과 시 요약문을 저장하고, 원문은 첨부/링크로 처리(사유에 요약 저장 사실 기재).

8) 트랜잭션 및 에러 처리(개선)
   - INSERT는 트랜잭션으로 감싸고, 실패 시 롤백.
   - 실패 시 등록상태=‘보류’, 사유에는 요약된 에러 메시지를 기록(민감정보 마스킹). 내부 세부 오류는 로그로만 보관.

## 절차 C. 운영 품질(개선)
9) 중복 실행 방지(개선)
   - 동일 작업 내 다중 툴 호출/중복 INSERT 시도 제거. 
   - 예: get_related_workitem_outputs 등은 1회 호출 후 캐시해 재사용.

10) Undo 사용 기준(신규)
   - 잘못된 중복 쓰기 발생 시에만 한정적으로 사용. 
   - 정상 플로우에서는 Undo 자동 호출을 억제.

11) 표준 포맷 통지(개선)
   - 등록 성공/보류 모두 1시간 내 이메일 통지. 본문에는 반드시 ‘결과 산출물’의 1행 요약 표를 포함.

## 출력
- 리뷰 단계(선택): 규칙 근거 중심의 리뷰 표/JSON이 필요할 때 기존 포맷을 사용 가능.
- 등록 단계(필수): 다음 열 순서의 마크다운 표 1개를 반환한다.
  - | 계정과목 | 금액 | 사용일 | 승인구분(룰 산출) | 등록상태(성공/보류) | 사유/메모(선택) |
- 보류 시 ‘사유/메모’에 보완 요청 항목 또는 초과 근거/미정의 사유를 구체적으로 기재.

## 예시(단건)
입력: account_code=SUP, amount=45,800 KRW, used_at=2026-09-03, applicant=홍길동, tenant_id=localhost, purpose="사무용품(프린터 토너) 구매"
- 규칙엔진 결과: is_over_limit=false → 승인구분=‘한도내 승인’
- proc_inst_id/todo_id 유효, 기등록 아님 → INSERT 성공

반환 표:
| 계정과목 | 금액 | 사용일 | 승인구분(룰 산출) | 등록상태(성공/보류) | 사유/메모(선택) |
| --- | ---: | --- | --- | --- | --- |
| SUP | 45,800 | 2026-09-03 | 한도내 승인 | 성공 |  |

예시(보류 - 컨텍스트 누락): proc_inst_id 누락 → 등록상태=보류, 사유=워크아이템 식별자 없음

## 품질 체크리스트(통합)
- 규칙엔진 evaluate('경비 한도 승인 판단') 호출로 승인구분 재평가 수행(한도 계산 목적의 DB 직접 조회 금지).
- 승인구분 매핑이 ‘한도내 승인/한도 초과/한도 미정의’로 일관되게 적용됨.
- proc_inst_id/todo_id 누락 시 보류 처리, 템플릿 플레이스홀더('{{...}}') 탐지 시 차단됨.
- 멱등성(기등록) 경로 검증: 동일 키 기등록 시 재삽입 금지 및 상태 표기(성공(기등록) 또는 보류(중복)).
- SQL 준비문/바인딩 사용, 문자열 길이 초과 시 요약 저장 및 원문 별도 보관.
- 실패 시 트랜잭션 롤백, 에러 메시지 마스킹.
- 동일 작업 내 중복 툴 호출/중복 INSERT 시도 없음(캐시 사용).
- Undo는 예외적 중복 쓰기 교정에만 사용, 정상 플로우에서 자동 호출 금지.
- 결과 표 형식·필드 정확히 채움. 이메일 통지 본문에 결과 표 포함.

## 하지 않는 것(안티패턴)
- 규칙엔진을 우회해 한도를 자의 계산하거나 DB 합계로 대체 판단.
- 승인구분이 ‘한도내 승인’이 아닌데 INSERT 시도.
- proc_inst_id/todo_id 미해소 또는 '{{...}}' 플레이스홀더 상태로 DB 실행.
- 준비문/바인딩 없이 문자열을 직접 SQL에 삽입.
- 동일 작업 내 다중 INSERT 시도, 무분별한 Undo 자동 실행.
